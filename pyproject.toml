[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "atelier"
version = "0.1.0"
description = "Atelier — deterministic, chunk-assembled polymorphic malware generation with autonomous EDR evasion"
requires-python = ">=3.10"

[[project.authors]]
name = ""

[tool.setuptools]
package-dir = {"atelier" = "."}
