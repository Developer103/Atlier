name: ad_recon_stealth
description: AD recon Stealth — minimal LDAP queries, max evasion, DCOnly

core:
  - ad/json_builder
  - ad/ldap_client
  - ad/sid_resolver
  - ad_collectors/ad_domains

collectors:
  - ad_collectors/ad_users
  - ad_collectors/ad_groups
  - ad_collectors/ad_computers

resources: true

evasion:
  - evasion/behavioral_pacing
  - evasion/sleep_jitter
  - evasion/anti_sandbox
  - evasion/deferred_exec

arch: arch/ad_recon

vars:
  C2_IP: "10.0.2.2"
  C2_PORT: "9001"
  LDAP_USER: "it.admin"
  LDAP_DOMAIN: "MALWARE"
  LDAP_PASS: "Adm1nP@ss!"
