name: ad_recon_dconly
description: AD recon DCOnly — LDAP-only enumeration, BloodHound v6 JSON output

core:
  - ad/json_builder
  - ad/ldap_client
  - ad/sid_resolver
  - ad_collectors/ad_domains

collectors:
  - ad_collectors/ad_users
  - ad_collectors/ad_groups
  - ad_collectors/ad_computers
  - ad_collectors/ad_ous

resources: true

evasion: []
arch: arch/ad_recon

vars:
  C2_IP: "10.0.2.2"
  C2_PORT: "9001"
  LDAP_USER: "it.admin"
  LDAP_DOMAIN: "MALWARE"
  LDAP_PASS: "Adm1nP@ss!"
